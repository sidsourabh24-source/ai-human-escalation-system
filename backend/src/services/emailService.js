export async function sendEscalationEmail({ conversationId, userMessage }) {
  // Placeholder for Nodemailer integration in Module 4.
  console.log("[email] escalation", { conversationId, userMessage });
}
